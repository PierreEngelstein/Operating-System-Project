
#define PRIVATE static   /* Limits the scope of the function to the file where it is defined */
#define PUBLIC
/* Boolean */
#define TRUE 1           /* Manage boolean with integers */
#define FALSE 0          /* Manage boolean with integers */

#define NULL ((void*)0)

/* Macros */
#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
